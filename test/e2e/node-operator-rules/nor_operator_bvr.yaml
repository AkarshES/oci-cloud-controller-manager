apiVersion: oci.oraclecloud.com/v1beta1
kind: NodeOperationRule
metadata:
  name: bvr
spec:
  actions:
    - replaceBootVolume
  maxParallelism: 2
  nodeEvictionSettings:
    evictionGracePeriod: 10
    isForceActionAfterGraceDuration: true
  nodeSelector:
    matchTriggerLabel:
      oke.oraclecloud.com/node_operation: bvr
